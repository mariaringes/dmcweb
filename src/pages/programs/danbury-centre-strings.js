import * as React from 'react';
import { Container } from 'react-bootstrap';
import Layout from '../../components/Layout';

export default function DCSPage() {
  return (
    <Layout>
      <Container>Danbury Centre Strings</Container>
    </Layout>
  );
}
